// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: echo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "echo.v1";

export interface SleepRequest {
  millis: number;
}

export interface RepeatRequest {
  message: string;
  count: number;
}

export interface EchoRequest {
  message: string;
}

export interface EchoResponse {
  message: string;
}

function createBaseSleepRequest(): SleepRequest {
  return { millis: 0 };
}

export const SleepRequest: MessageFns<SleepRequest> = {
  encode(message: SleepRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.millis !== 0) {
      writer.uint32(8).uint32(message.millis);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SleepRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSleepRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.millis = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SleepRequest {
    return { millis: isSet(object.millis) ? globalThis.Number(object.millis) : 0 };
  },

  toJSON(message: SleepRequest): unknown {
    const obj: any = {};
    if (message.millis !== 0) {
      obj.millis = Math.round(message.millis);
    }
    return obj;
  },

  create(base?: DeepPartial<SleepRequest>): SleepRequest {
    return SleepRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SleepRequest>): SleepRequest {
    const message = createBaseSleepRequest();
    message.millis = object.millis ?? 0;
    return message;
  },
};

function createBaseRepeatRequest(): RepeatRequest {
  return { message: "", count: 0 };
}

export const RepeatRequest: MessageFns<RepeatRequest> = {
  encode(message: RepeatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.count !== 0) {
      writer.uint32(16).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RepeatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepeatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RepeatRequest {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: RepeatRequest): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<RepeatRequest>): RepeatRequest {
    return RepeatRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RepeatRequest>): RepeatRequest {
    const message = createBaseRepeatRequest();
    message.message = object.message ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseEchoRequest(): EchoRequest {
  return { message: "" };
}

export const EchoRequest: MessageFns<EchoRequest> = {
  encode(message: EchoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EchoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEchoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EchoRequest {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: EchoRequest): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<EchoRequest>): EchoRequest {
    return EchoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EchoRequest>): EchoRequest {
    const message = createBaseEchoRequest();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseEchoResponse(): EchoResponse {
  return { message: "" };
}

export const EchoResponse: MessageFns<EchoResponse> = {
  encode(message: EchoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EchoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEchoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EchoResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: EchoResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<EchoResponse>): EchoResponse {
    return EchoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EchoResponse>): EchoResponse {
    const message = createBaseEchoResponse();
    message.message = object.message ?? "";
    return message;
  },
};

export type EchoDefinition = typeof EchoDefinition;
export const EchoDefinition = {
  name: "Echo",
  fullName: "echo.v1.Echo",
  methods: {
    /**
     * Unary: returns the same message. Also exposed as REST via HTTP transcoding:
     *   POST /v1/echo          { "message": "hi" }
     *   GET  /v1/echo/{message}
     */
    unary: {
      name: "Unary",
      requestType: EchoRequest as typeof EchoRequest,
      requestStream: false,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              35,
              34,
              8,
              47,
              118,
              49,
              47,
              101,
              99,
              104,
              111,
              58,
              1,
              42,
              90,
              20,
              18,
              18,
              47,
              118,
              49,
              47,
              101,
              99,
              104,
              111,
              47,
              123,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
    /**
     * Bidi streaming: echoes each message back. (Un-annotated: reached only over the
     * main WebSocket surface.)
     */
    stream: {
      name: "Stream",
      requestType: EchoRequest as typeof EchoRequest,
      requestStream: true,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: true,
      options: {},
    },
    /**
     * Bidi streaming echo, exposed as a REST-transcoded stream:
     *   POST /v1/chat   (each frame's JSON body is a request message)
     */
    chat: {
      name: "Chat",
      requestType: EchoRequest as typeof EchoRequest,
      requestStream: true,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: true,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([13, 34, 8, 47, 118, 49, 47, 99, 104, 97, 116, 58, 1, 42]) as Uint8Array],
        },
      },
    },
    /**
     * Server streaming that emits `message` `count` times, then completes. REST:
     *   GET /v1/repeat/{message}?count=N  (request built entirely from the URL)
     */
    repeat: {
      name: "Repeat",
      requestType: RepeatRequest as typeof RepeatRequest,
      requestStream: false,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: true,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              22,
              18,
              20,
              47,
              118,
              49,
              47,
              114,
              101,
              112,
              101,
              97,
              116,
              47,
              123,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
    /**
     * Server streaming that emits one message then hangs forever; used to observe
     * upstream cancellation when the client resets/disconnects.
     */
    hang: {
      name: "Hang",
      requestType: EchoRequest as typeof EchoRequest,
      requestStream: false,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: true,
      options: {},
    },
    /**
     * Unary that sleeps `millis` before replying; used to exercise deadlines.
     * REST: GET /v1/sleep?millis=N  (numeric field bound from a query param).
     */
    sleep: {
      name: "Sleep",
      requestType: SleepRequest as typeof SleepRequest,
      requestStream: false,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([11, 18, 9, 47, 118, 49, 47, 115, 108, 101, 101, 112]) as Uint8Array],
        },
      },
    },
    /**
     * Unary that fails with UNAVAILABLE a preset number of times, then succeeds;
     * used to exercise retry.
     */
    flakyUnary: {
      name: "FlakyUnary",
      requestType: EchoRequest as typeof EchoRequest,
      requestStream: false,
      responseType: EchoResponse as typeof EchoResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
